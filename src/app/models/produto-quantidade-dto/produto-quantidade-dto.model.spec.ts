import { ProdutoQuantidadeDto } from './produto-quantidade-dto.model';

describe('ProdutoQuantidadeDto', () => {
  it('should create an instance', () => {
    expect(new ProdutoQuantidadeDto()).toBeTruthy();
  });
});
