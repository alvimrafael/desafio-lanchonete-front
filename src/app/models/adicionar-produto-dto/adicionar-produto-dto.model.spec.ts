import { AdicionarProdutoDto } from "./adicionar-produto-dto.model";

describe('AdicionarProdutoDto', () => {
  it('should create an instance', () => {
    expect(new AdicionarProdutoDto()).toBeTruthy();
  });
});
